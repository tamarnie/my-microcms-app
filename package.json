{
  "name": "hanare-website",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "prebuild": "node scripts/prebuild.js",
    "build": "npm run prebuild && mkdir -p dist && cp index.html dist/ && cp -r public/* dist/",
    "deploy": "vercel --prod",
    "health-check": "node scripts/health-check.js"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}